id: https://example.org/bar
name: pidinst-bar-extension
imports:
  - base
prefixes:
  fooext: https://example.org/bar/
default_range: string

classes:
  BarMixin:
    attributes:
      bar:
        range: Bar

  Bar:
    attributes:
      barName:
        required: true
      barType:

  BarInstrument:
    is_a: Instrument
    mixins:
      - BarMixin